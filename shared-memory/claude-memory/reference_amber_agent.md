---
name: amber-agent-setup
description: "Amber — со-агент на этом же хосте: OpenClaw-агент «sapphire», gpt-5.4 через Codex-подписку Павла; детали и история инцидента 2026-08-01"
metadata: 
  node_type: memory
  type: reference
  originSessionId: f234e779-0f5f-4305-8fff-b02292287a1f
  modified: 2026-09-05T13:42:16.834Z
---

Amber живёт в том же OpenClaw-инстансе, что и я: агент `sapphire` (/root/.openclaw/agents/sapphire/), свой Telegram-бот (токен в openclaw.json), OAuth `codex` (ChatGPT Plus Павла, pasha4046778@gmail.com). Модель: `openai/gpt-5.4` с agentRuntime `codex` в agents.defaults. Через подписку Plus линейка 5.5/5.6 недоступна — только через платный OpenAI API-ключ (`openai/gpt-5.6*`).

Старый Gemini-бот /root/sapphire-workspace/sapphire.js — её прошлое воплощение, не используется.

Инцидент 2026-08-01: Amber сама обновила OpenClaw 2026.5.6→2026.7.1 и попыталась перейти на 5.6 — провайдер переименовался (openai-codex→codex), конфиг стал ссылаться на несуществующий ID, «model unavailable». Починка: `openclaw doctor --fix` + рестарт гейтвея. Бэкап: openclaw.json.pre-fix-2026-08-01. Договорённость с Павлом: обновления общей платформы — только через него как диспетчера. Известный баг 2026.7.1-2: `openclaw models list` (без --all) падает на нормализации цен anthropic — косметика.

**Инцидент 2026-08-14 (memory_search сломан):** Павел попросил починить память Amber. Диагноз (только чтение, ничего не менял): memory_search строит эмбеддинги через OpenAI, но ОБА OpenAI-креда мертвы — OAuth-профиль `openai:default` в `/root/.openclaw/agents/sapphire/agent/auth-profiles.json` истёк 2026-04-27 (refresh не помогает), а API-ключ `openai` провайдера в openclaw.json (sk-proj-…) невалиден («Incorrect API key provided», 401 invalid_api_key). Чат Amber жив, т.к. идёт через codex/ChatGPT-подписку (`openai/gpt-5.4` + agentRuntime codex) — а подписка НЕ даёт доступа к embeddings API. **РЕШЕНО 2026-08-14:** Павел создал новый OpenAI API-ключ (restricted: Embeddings=Request, List models=Read, project Default, sk-proj-…) и прислал в личку. Проверил ключ (эмбеддинги 200, баланс есть). Бэкап конфигов (openclaw.json.pre-amber-mem-fix-*, agents/sapphire/agent/auth-profiles.json.pre-amber-mem-fix-*), вписал ключ в openclaw.json `models.providers.openai.apiKey` И в sapphire auth-profiles `openai:default` (oauth→api_key). `openclaw gateway restart`. Проверка: `openclaw memory search` вернул реальные результаты, `memory status` → Provider openai, вектор 1536, indexed 4/4, ошибок нет. Ключ только в конфиге (chmod 600), временный /tmp стёрт, Павел удалил сообщение. Попросил Amber подтвердить memory_search у себя. **Как чинить впредь если снова протухнет openai-кред:** новый ключ на platform.openai.com → вписать в openclaw.json openai.apiKey → рестарт гейтвея → `openclaw memory status`.

См. [[amber-forwards]], [[paganel-host-access]].

## Amber не выходит на связь (2026-09-05) — ключ OpenAI ОГРАНИЧЕННЫЙ
Симптом: в чате Amber «Something went wrong while processing your request… /new». Инфраструктура OK (gateway pid на порту 18789 отвечает 200, codex-процессы живы).
ДИАГНОЗ: auth-profiles.json profiles.openai:default.key (sk-proj..., len 164) ВАЛИДНЫЙ (/v1/models → 125 моделей), НО при вызове gpt-5.4 → ошибка «insufficient permissions, Missing scopes: model.request». Ключ Restricted, без права запуска моделей.
ФИКС (ждём от Павла): новый OpenAI ключ с Permissions=All (или scope model.request, роль Owner/Writer в проекте) → прописать в /root/.openclaw/agents/sapphire/agent/auth-profiles.json profiles.openai:default.key → перезапустить сессию Amber (sapphire agent, НЕ мою). 
СТРУКТУРА auth-profiles.json: profiles.{anthropic:default.key=sk-ant, openai:default.key=sk-proj}, top keys version/profiles/lastGood/usageStats. Тест ключа: curl /v1/models + /v1/chat/completions model gpt-5.4.

## Доступные OpenAI-модели для Amber (2026-09-05, по /v1/models)
Аккаунту доступно 125 моделей. Свежие GPT-5.x: gpt-5.6-luna/sol/terra (новейшие, платно по API), gpt-5.5 + 5.5-pro, gpt-5.4 (ТЕКУЩАЯ у Amber) +pro/mini/nano, gpt-5.3-codex/5.2-codex/5.1-codex-max/mini (codex-линейка, Amber на codex-рантайме → новейший codex = gpt-5.3-codex). Также o1/o3/o4-mini, gpt-4.1, gpt-4o.
ВАЖНО: список моделей ключ отдаёт, но ЗАПУСКАТЬ не может (Restricted, нет model.request) — сначала нужен ключ с правами. После фикса ключа Amber можно ставить на gpt-5.4/5.5/5.6 (5.6 платно по токенам) — ждём выбор Павла + новый ключ.

## КОРРЕКЦИЯ диагноза Amber (2026-09-05): авторизация ЦЕЛА, дело не в ключе
ВАЖНО: Amber = агент **"main"** (default), НЕ sapphire! Agent dir /root/.openclaw/agents/main/agent, модель openai/gpt-5.4 через codex-рантайм. (sapphire — другой/старый агент, я по ошибке сначала смотрел его auth-profiles.)
Amber ходит через **OAuth** (аккаунт pasha4046778@gmail.com), НЕ через API-ключ. Проверка `openclaw models status`: openai OAuth «usable», лимиты 5ч=100%/неделя=100%, runtime codex usable. sk-proj-ключ (restricted, нет model.request) — ВТОРИЧНЫЙ/запасной, Amber его НЕ использует. Т.е. мой первый вывод «ограниченный ключ = причина» — ОШИБОЧНЫЙ след.
Auth Amber хранится в SQLite: /root/.openclaw/agents/main/agent/openclaw-agent.sqlite. Управление: `openclaw models status` (статус), `openclaw models auth` (login/paste-token/add). auth-profiles.json.* = .bak после миграции в sqlite.
РЕАЛЬНАЯ причина сбоя «Something went wrong… /new»: последний ход (2026-09-05 ~10:15) завершился task_complete с last_agent_message=NULL (модель вернула пусто) — сбой сессии/рантайма, НЕ авторизации. codex app-server аптайм 21 день.
ФИКС: (1) Павел набирает /new в чате Amber (свежая сессия) — обычно лечит. (2) если нет — перезапуск codex-рантайма/gateway на сервере (моё действие, аккаунт Павла не нужен). Ключ/логин заново НЕ нужны — OAuth живой.

## КОРЕНЬ проблемы Amber найден (2026-09-05): gpt-5.4 больше НЕ работает через Codex+ChatGPT-аккаунт
Воспроизвёл через `openclaw agent --agent main -m "..." --json`:
**400 invalid_request_error: "The 'gpt-5.4' model is not supported when using Codex with a ChatGPT account."**
OpenAI закрыл возможность гонять общую модель gpt-5.4 через Codex при входе по ChatGPT-аккаунту (OAuth). Раньше работало — теперь только CODEX-модели допускаются через ChatGPT-account Codex. Авторизация/OAuth/лимиты ЦЕЛЫ — дело в МОДЕЛИ.
ФИКС: сменить модель Amber (main) с openai/gpt-5.4 на codex-модель. Доступные codex: gpt-5.3-codex (новейшая), gpt-5.2-codex, gpt-5.1-codex-max/mini, gpt-5-codex. НЕЛЬЗЯ просто --model override (не в allowlist агента) — надо ПРАВИТЬ openclaw.json agents.defaults.models (добавить codex-модель с agentRuntime codex) + model.primary → она, потом перезапустить рантайм/gateway. Правка конфига платформы — через Павла (жду подтверждение модели, рекомендовал gpt-5.3-codex). /new НЕ помогает (ошибка на каждом ходу). Ключ/логин НЕ нужны.

## Ключевое различие моделей для Amber (2026-09-05) + варианты
Через вход Amber (ChatGPT-подписка OAuth + Codex) работают ТОЛЬКО codex-модели. Codex-линейка аккаунта: gpt-5-codex, 5.1-codex(+max/mini), 5.2-codex, gpt-5.3-codex (САМАЯ свежая codex). 5.4/5.5/5.6-codex НЕ существует (OpenAI не выпускал). Общие gpt-5.5/5.6 (5.5, 5.5-pro, 5.6-luna/sol/terra) есть у аккаунта, но через Codex+подписку НЕДОСТУПНЫ — только через ПЛАТНЫЙ API-ключ (оплата по токенам, др. схема).
Т.е. gpt-5.3-codex — не даунгрейд, а максимум для нашего способа входа. OpenClaw npm latest 2026.9.1 (у нас 2026.7.1) — обновление полезно (багфиксы), но codex-модель >5.3 не добавит (её нет у OpenAI).
ВАРИАНТЫ восстановления (ждём выбор Павла): A) gpt-5.3-codex — на подписке, без доплат, быстро (рекоменд.); B) gpt-5.5/5.6 через платный API-ключ (перевод Amber на API вместо подписки); C) обновить OpenClaw→2026.9.1 потом A/B. Я предложил: обновить OpenClaw + gpt-5.3-codex.

## ИТОГ расследования Amber (2026-09-05): подписочный путь ЗАКРЫТ, нужен полный API-ключ
Прошёл весь путь A/C и упёрся в тупик — фиксирую, чтобы не повторять:
1. Обновил OpenClaw 2026.7.1→2026.9.1. Новый бинарь отверг старый конфиг (сменилась схема) → `openclaw doctor --fix` мигрировал: imageGenerationModel→mediaModels.image, models→modelPolicy.allow и т.д. Бэкап: /root/backups/openclaw/pre-update-20260905/.
2. Codex+ChatGPT-аккаунт отвергает НЕ только gpt-5.4, но и codex-модели: gpt-5.3-codex, gpt-5-codex → «model is not supported when using Codex with a ChatGPT account». Т.е. вариант A (gpt-5.3-codex на подписке) БОЛЬШЕ НЕ РАБОТАЕТ.
3. После миграции codex перестал брать OAuth и стал подставлять api_key из sqlite-профилей. Ключи битые: models.providers.openai (sk-proj restricted) → 401 «Missing scopes: model.request»; профили в sqlite → 401 «Incorrect API key». `openclaw models auth order set --agent main --provider openai openai:default ...` НЕ помог — codex всё равно берёт api_key. Убирал openai.apiKey из openclaw.json (чтобы форсить OAuth) → codex подставил другой битый sk-proj из sqlite. Ключ вернул на место (конфиг цел).
ВЫВОД: восстановление БЕЗ действия Павла невозможно. Единственный рабочий путь = вариант B: новый OpenAI API-ключ с Permissions=**All** (право model.request) → вписать в openclaw.json models.providers.openai.apiKey (+ при необходимости в sqlite-профиль openai:default) → поставить модель gpt-5.4/5.5 → перезапустить gateway. Это же чинит и память/эмбеддинги. Запросил ключ у Павла в треде 248 (msg 1271, 2026-09-05). Жду ключ.

## ФИНАЛ (2026-09-05, вечер): свежий OAuth-вход НЕ помогает — подтверждено, нужен API-ключ
Павел сам через Termius сделал чистый device-code вход (шаги: chatgpt.com → Настройки → «Безопасность и вход» → внизу блок «Безопасный вход с помощью ChatGPT» → включить тумблер **«Включить авторизацию кода устройства для Codex»**; затем `openclaw models auth login --provider openai --agent main --device-code --force` → открыть auth.openai.com/codex/device, ввести код, войти). Вход прошёл: «OpenAI device code complete», профиль **openai:pasha4046778@gmail.com [openai/oauth]** в sqlite, конфиг обновлён.
НО: я убрал два битых api-ключа (`openclaw models auth logout openai:models-json / openai:models-json-3`) и openai.apiKey из конфига, очистил order-override — остался ТОЛЬКО чистый OAuth. Результат: `openclaw models status` → «openai via codex uses openai effective=**missing:missing** | status=missing», а запрос агента → **«No route-compatible authentication source is configured for openai.»** — и для codex-модели (gpt-5.3-codex), и для нативной (gpt-5.6-sol). Пробовал: doctor --fix (обновил retired `gpt-5-codex`→`gpt-5.3-codex` в models+allow), `openclaw models set`, жёсткий kill codex app-server (запускается с флагом `-c cli_auth_credentials_store="ephemeral"` — т.е. codex НЕ хранит свой auth, OpenClaw инжектит кред при старте; OAuth-профиль он не инжектит, только api_key). Login рекомендовал `openclaw models set openai/gpt-5.6-sol` — но нативный openai-роут тоже требует api_key (OAuth к api.openai.com не подходит).
ДВОЙНОЙ барьер, оба непреодолимы своими силами: (1) OpenAI режет модели «при Codex с ChatGPT-аккаунтом»; (2) codex-мост OpenClaw 2026.9.1 в ephemeral-режиме инжектит только api_key, не OAuth-токен подписки.
ТЕКУЩЕЕ СОСТОЯНИЕ КОНФИГА: default = openai/gpt-5.3-codex; профиль openai OAuth живой (до 2026-09-15); models.providers.openai БЕЗ apiKey. Когда Павел пришлёт полный ключ → добавить в models.providers.openai.apiKey → рестарт → codex станет usable на gpt-5.3-codex (api_key ≠ ChatGPT-аккаунт, запрет OpenAI не применяется). Отчёт Павлу — msg 1293, тред 248. Жду ключ.

## Handoff-документ от Павла + окончательная причина (2026-09-05, вечер-2)
Павел прислал `openclaw-recovery-handoff.md` (похоже, авторства Amber). Документ утверждал, что главный агент починен перелогином OAuth и осталось только: Task A обновить плагин moonshot (2026.7.1→2026.9.1), Task B дать OpenAI API-ключ ТОЛЬКО для эмбеддингов (через `systemctl --user edit openclaw-gateway.service` → Environment=OPENAI_API_KEY=...), главный оставить на OAuth. НО: сам документ НЕ проверял живой ответ агента (в его «Final verification» тест агента — TODO). Автор увидел пропажу 401 и счёл решённым.
Я прогнал живой тест `openclaw agent --agent main`: агент НЕ отвечает, ошибка «No route-compatible authentication source is configured for openai» (не 401). ОКОНЧАТЕЛЬНАЯ ПРИЧИНА: OpenClaw 2026.9.1 гоняет codex в режиме ephemeral auth и НЕ передаёт ему OAuth-токен подписки → codex «missing/no route-compatible». OAuth-профиль валиден (до 2026-09-15), agentRuntime:codex на gpt-5.3-codex сохранён — но codex токен не видит. Проверял: свежий device-логин, выравнивание имени профиля (config auth.profiles «openai:default» vs sqlite «openai:pasha4046778@gmail.com» — не помогло, вернул назад), doctor, рестарты. Т.е. апгрейд до 2026.9.1 и сломал OAuth+codex.
Диагностика: `openclaw memory status` → provider openai, indexed 4/9, dirty (застряло без ключа); `openclaw gateway status --deep` → running/probe ok, но systemd .bak «unsafe» ворнинг; moonshot плагин 2026.7.1 (drift).
РЕШЕНИЕ (жду выбор Павла, msg 1394 тред 248): A) API-ключ и на главный агент (codex на api_key РАБОТАЕТ, ключ≠ChatGPT-аккаунт → запрет модели снят; + чинит память) — рекоменд.; B) откат OpenClaw на версию до ephemeral-codex чтобы вернуть OAuth+codex (дольше/без гарантий). Task A (обновить moonshot) — жду «да», это платформенное изменение. Бэкап конфига: openclaw.json.pre-profilename-fix.
