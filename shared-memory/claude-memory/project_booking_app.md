---
name: booking-app-project
description: "Своя система онлайн-записи для ногтевой студии (упрощённый DIKIDI) — новый проект Павла, старт 2026-07-30"
metadata: 
  node_type: memory
  type: project
  originSessionId: f234e779-0f5f-4305-8fff-b02292287a1f
  modified: 2026-08-04T09:33:16.614Z
---

2026-07-30 Павел поставил задачу (топик Dev-Разработка): сделать аналог DIKIDI с меньшим функционалом для ногтевой студии. Требования: веб-версия, «приложения» для админа и мастеров, рассылка уведомлений клиентам/админам/мастерам. Точный список функций Павел пришлёт отдельно — до этого код не начинать.

Стек: Next.js + Postgres на VPS 46.8.79.53, приложения мастера/админа как PWA, уведомления Telegram-бот + web-push. Этапы: ТЗ → ядро слотов → клиентская запись → админ-журнал → кабинет мастера+уведомления → PWA/деплой. Рабочая запись ~неделя после ТЗ, MVP 3–4 недели.

**2026-08-01: собрал всю панель DIKIDI (37 разделов) через доступ Павла и написал черновик ТЗ → research/booking-app-tz.md** (отправлен Павлу файлом). Важно: студия из панели — «Галерея Красоты» Марии Бабич-Гудебской (Актобе), тот же фруктовый педикюр, что FrutPed — т.е. проект для реальной студии, данные живые (19 услуг, мастера, клиентская база).

**2026-08-01 решения Павла:** (1) мультистудийность ДА; (2) домен другой, готовит сам (я предложил 5: zapishi.me/bronly.app/slotim.kz/mastira.com/naillook.app); (3) предоплата НЕ в MVP; (4) групповая запись НЕТ; (5) уведомления — приоритет **WhatsApp** (нужен шлюз, не бесплатно; рекомендовал GREEN-API ~2-4к тг/мес); (6) русский + тенге. Дал команду «приступай к верстке, деплой на VPS».

**Сборка (началась 2026-08-01):** Next.js 16 + React 19 + Tailwind 4, проект в `/root/booking-app`. Готова клиентская форма записи (услуга→мастер→дата/время→контакт→подтверждение) на моках реальной студии (src/lib/studio.ts — 19 услуг, 9 мастеров Галереи Красоты). Задеплоено: systemd `booking-app.service` на порту 3002, превью открыто наружу `http://46.8.79.53:3002` через **временное ufw-правило 3002/tcp (ЗАКРЫТЬ после демо/переезда на домен)**. **2026-08-01 (2): по просьбе Павла развёрнута ВСЯ структура админки** — 29 разделов из DIKIDI в боковом меню по 9 группам (реестр src/lib/sections.ts). Роуты: `/admin/[section]` (SSG), `/admin`→journal. Полностью рабочие экраны: journal, services, masters, clients (фильтры по сегментам), schedule; остальные — generic-заглушка SectionStub из реестра (таблицы/настройки/KPI/списки), помечены «в MVP»/«позже». Дизайн переоформлен в трендовую палитру: фиолет #6d4aff + тёплые нейтральные + розовый акцент #ff6b9d (globals.css @theme токены, studio.accent тоже фиолет). Клиентская форма в новых цветах. Компоненты: src/components/admin/*, src/lib/clients.ts (мок базы). Всё билдится, задеплоено на :3002.

**2026-08-01 (3): по просьбе Павла — все разделы доведены до реального функционала + усилена палитра.** Каждый из 29 разделов теперь настоящий экран (не заглушка): таблицы с данными, фильтры, вкладки, настройки-тумблеры, KPI-плитки, мини-графики (Bars/Donut на CSS). Компоненты в src/components/admin/sections/{online,notify,finance,clients,goods,loyalty,settings}.tsx + общие примитивы src/components/admin/ui.tsx (Card, StatTile, Pill, Toggle, DataTable, Bars, Donut, SettingCard/Row). Палитра выразительнее: градиенты (grad-primary фиолет→розовый, grad-sidebar тёмный), цвета по группам (GROUP_COLORS в sections.ts), тёмная боковая панель с цветными активными иконками. Всё билдится (Next 16, 34 страницы SSG), задеплоено :3002. Фидбек Павла: «смотрится хорошо, делай основательно» — сделал.

**2026-08-01 (4): Павел попросил «в точности как DIKIDI + нативные iOS/Android», дедлайн 4ч20м, «жду готовый продукт».** Честно ответил: точная копия DIKIDI (12 лет разработки) + отдельные нативные приложения за 4ч невозможны; предложил реальный максимум и сделал:
- **PWA** — приложение устанавливается на телефон (manifest.webmanifest, sw.js, иконки-градиент ГК в public/icons, apple-meta в layout, PWA.tsx регистрирует SW). ВАЖНО: настоящая установка/offline/push требуют HTTPS — на http-превью :3002 работает только «на экран Домой» + fullscreen; полноценно включится на домене с HTTPS.
- **Реальное сохранение записей**: API `/api/bookings` (GET/POST) + файловое хранилище src/lib/store.ts (data/bookings.json). Клиентская форма (BookingFlow) постит запись; журнал (Journal) читает и показывает онлайн-записи за сегодня + бейдж «N онлайн-записей». Ручная запись из журнала («+ Запись» → модалка AddBookingModal) тоже сохраняется. Оба цикла реально работают.
- **Кнопки/тосты**: PrimaryButton/GhostButton дают тост-подтверждение (Toaster в ui.tsx), чтобы не было «мёртвых» кнопок; честный текст «подключается к базе на следующем шаге».

**2026-08-02: Павел согласился идти step-by-step по разделам по порядку; домен готовит сегодня.** Начал углублять разделы по очереди.

**Раздел 1 «Журнал записей» — углублён до логики DIKIDI (готово):** навигация по датам (←/→/«Сегодня», подгрузка записей за выбранный день), создание записи кликом по пустому слоту (мастер+время подставляются), «+ Запись» с переключателем Запись/Занять время, блок времени (штриховка, причина), детальная карточка с переключением статуса, переносом (Перенести на время), отменой и удалением. Всё сохраняется на бэкенд: API `/api/bookings` расширен PATCH (статус/время) и DELETE; store.ts — updateBooking/removeBooking, статус cancelled, kind block/appointment. Проверено CRUD end-to-end. Файлы: src/components/Journal.tsx (переписан), src/lib/appointments.ts (+cancelled, NEXT_STATUS), src/lib/store.ts, src/app/api/bookings/route.ts.

**Раздел 2 «График работы» (готово 2026-08-02):** недельная сетка с реальными датами (←/→ недели, «Эта неделя»), выбор ячеек мастер×день (мультивыбор), нижняя панель «Задать часы»/«Выходной»/сброс, редактор часов (начало/конец). Эффективные часы = переопределение или базовый шаблон master.hours. Сохранение: API `/api/schedule` (GET/POST batch) + src/lib/schedule.ts (data/schedule.json, ключ masterId|date → {start,end}|null). Проверено. Файлы: src/components/admin/ScheduleAdmin.tsx (переписан), src/lib/schedule.ts, src/app/api/schedule/route.ts.

**Раздел 3 «Услуги» (готово 2026-08-02):** полный CRUD — вкладки Активные/Архив со счётчиками, модалка добавления/редактирования (название, категория, длительность, цена, «от», онлайн-видимость), клик по плашке онлайн/скрыта переключает видимость, архив/возврат. Сохранение поверх сид-данных: API `/api/services` (GET + POST action add/edit/archive) + src/lib/services-store.ts (data/services.json: {edits, added, archived}, мержится с seed). Проверено. Файлы: src/components/admin/ServicesAdmin.tsx (переписан), src/lib/services-store.ts, src/app/api/services/route.ts.

Паттерн для след. разделов: store поверх сид-данных (JSON-патч) + API GET/POST action + клиент с оптимистичным UI.

**Раздел 4 «Сотрудники» (готово 2026-08-02):** CRUD мастеров — вкладки Работают/Архив, модалка (имя, должность из списка, доступность онлайн, мультивыбор оказываемых услуг по категориям с «выбрать/снять все»), архив/возврат. API `/api/masters` + src/lib/masters-store.ts (data/masters.json, паттерн как services). Проверено. Файлы: src/components/admin/MastersAdmin.tsx (переписан), src/lib/masters-store.ts, src/app/api/masters/route.ts.

**Раздел 5 «Ресурсы» (готово 2026-08-02):** CRUD рабочих мест/оборудования — таблица (название, тип, одновременно, категории услуг), модалка добавления/редактирования, удаление. Standalone-store (не патч, а полный список с дефолтным сидом 5 ресурсов): src/lib/resources-store.ts (data/resources.json), API `/api/resources`, компонент src/components/admin/ResourcesAdmin.tsx; в dispatcher resources→ResourcesAdmin (заменил заглушку из settings.tsx, там Resources теперь неиспользуемый экспорт). Проверено.

**Группа «Записи» полностью готова (разделы 1–5).**

**Раздел 6 «Клиентская база» (готово 2026-08-02):** поиск по имени/телефону, фильтры по сегментам, +Клиент (модалка: имя, телефон, скидка, сегмент, комментарий), клик по строке → карточка клиента (статы визитов/чек/потрачено, история визитов, скидка, комментарий, кнопки Изменить и Чёрный список), экспорт CSV (Blob-скачивание), импорт CSV (парсинг name,phone → добавление). Store поверх сида: src/lib/clients-store.ts (data/clients.json {edits, added}), API `/api/clients`, компонент ClientsAdmin.tsx (переписан), в clients.ts добавлены поля discount?/comment?. Проверено. NB: история визитов пока синтезируется из счётчика — реальные визиты привяжем, когда журнал будет писать визиты в карточку.

**Раздел 7 «Возвращаемость» (готово 2026-08-02):** data-driven — тянет реальных клиентов из `/api/clients`, считает сегменты динамически; кликабельные KPI-плитки (переключают drill-down), донат распределения из реальных данных, настройка порогов сегментов, список клиентов выбранного сегмента с кнопкой «Запустить рассылку» (для заснувших/потерянных, ведёт в Рассылки — тост). Файл: src/components/admin/sections/clients.tsx (Retention переписан). Проверено.

**Раздел 8 «Отзывы» (готово 2026-08-02):** список отзывов из store, метрики (средний рейтинг/кол-во/без ответа — считаются), фильтр по звёздам (все/5+/4+/3+), инлайн-ответ студии на отзыв с сохранением на сервер. Store с сидом 6 отзывов: src/lib/reviews-store.ts (data/reviews.json), API `/api/reviews` (GET + POST reply), компонент Reviews в sections/clients.tsx (переписан). Проверено (ответ персистится).

**Раздел 9 «Чат» (готово 2026-08-02):** двухпанельный чат — список диалогов (с бейджами непрочитанных, превью последнего сообщения) + тред с историей, отправка сообщения (Enter/кнопка) с сохранением, авто-сброс unread при открытии, адаптив (на мобиле список↔тред). Store с 3 сид-диалогами: src/lib/chat-store.ts (data/chat.json), API `/api/chat` (GET + POST send/read), компонент Chat в sections/clients.tsx (переписан). Проверено.

**Группа «Клиенты» готова целиком (разделы 6–9).**

**Раздел 10 «Настройки онлайн-записи» (готово 2026-08-02):** вкладка Настройки теперь controlled и сохраняется — статус (активна/ЧС/предоплата), авто-подбор мастера, шаг сетки, языки (RUS/ENG/KAZ тумблеры), согласие ПДн; вкладка Оферта — текст сохраняется; кнопка «Сохранить» пишет на сервер (toast). Создан **универсальный settings-store** (неймспейсы online/notifications/alerts/bonuses/profile с дефолтами): src/lib/settings-store.ts (data/settings.json), API `/api/settings` (GET ?ns= + POST {ns,patch}), хук `useSettings(ns)` в ui.tsx, Toggle стал controlled-capable (checked/onChange). Компонент Online в sections/online.tsx переписан. Проверено. Вкладки «Правила бронирования» и «Дизайн»-цвета пока визуальные (правила — отдельная сущность, сделаю CRUD позже). NB: этот settings-store переиспользуется для разделов 11–? (Уведомления, Оповещения, Бонусы, Профиль) — там просто подключить useSettings(ns) с уже готовыми дефолтами.

**Раздел 11 «Каналы и виджеты» (готово 2026-08-02):** копирование прямой ссылки (clipboard+toast), генерация QR-кода (библиотека `qrcode`, dynamic import, модалка + скачивание PNG), код виджета-iframe (модалка + копирование), ссылки на отдельные услуги (модалка: список услуг, копировать ссылку/QR на каждую). Ссылка = window.location.origin (динамически, на домене будет правильной). Файл: Widgets в sections/online.tsx (переписан). Установлен qrcode + @types/qrcode. Проверено.

**Группа «Онлайн-запись» готова (разделы 10–11).**

**Раздел 12 «Уведомления клиентам» (готово 2026-08-02):** 7 типов уведомлений — controlled-тумблеры через useSettings('notifications'), кнопка Сохранить пишет на сервер, выбор канала (WhatsApp/Telegram/SMS), вкладка Статистика осталась. Файл: Notifications в sections/notify.tsx (переписан). Проверено.

**Разделы 13 «Оповещения» и 14 «Рассылки» (готово 2026-08-02):** Alerts — тумблеры мастеру/админу через useSettings('alerts'), сохранение. Mailing — таблица кампаний из store, модалка «Новая рассылка» (название, сегмент, канал, текст) считает размер аудитории из реальной базы `/api/clients`, создаёт кампанию (отправить/в черновик). Store: src/lib/mailings-store.ts (data/mailings.json), API `/api/mailings`. Файлы: Alerts+Mailing в sections/notify.tsx. Проверено.

**Группа «Уведомления» готова (разделы 12–14).**

**Раздел 15 «Продажи» (готово 2026-08-02):** список продаж из store, KPI считаются вживую (кол-во, выручка, средний чек, доля безнала), модалка «Оформить продажу» (услуга из каталога с ценой, мастер, оплата Kaspi/Карта/Наличные/Перевод, клиент) → создаёт продажу, KPI обновляются. Store: src/lib/sales-store.ts (data/sales.json, сид 4 продажи), API `/api/sales`. Файл: Sales в sections/finance.tsx. Проверено.

**Раздел 16 «Аналитика» (готово 2026-08-02):** связана с реальными продажами `/api/sales` — KPI (выручка/продажи/средний чек), Bars+Donut по способам оплаты, горизонтальные бары выручки по мастерам (агрегация из продаж). Пустые состояния если продаж нет. Файл: Analytics в sections/finance.tsx. Проверено.

**Раздел 17 «Финансы/ДДС» (готово 2026-08-02):** учёт доходов/расходов — KPI (доходы/расходы/чистая прибыль считаются вживую), фильтр по счёту, модалка операции (тип доход/расход с разными категориями, счёт, сумма) → добавляет, баланс пересчитывается. Store: src/lib/finance-store.ts (data/finance.json), API `/api/finance`. Файл: Finance в sections/finance.tsx. Проверено.

**Раздел 18 «Платежи» (готово 2026-08-02):** провайдеры оплаты (Kaspi/ЮKassa/Prodamus), предоплата (вкл + размер), кассы ККМ (фискализация + оборудование) — тумблеры/селекты через useSettings('payment'), сохранение. Добавлены дефолты payment в settings-store. Файл: Payment в sections/finance.tsx. Проверено.

**Группа «Финансы» готова (разделы 15–18).**

**Разделы 19 «Товары» и 20 «Склад» (готово 2026-08-02):** общий store goods (товары + складские операции). Товары: CRUD (название, категория, остаток, цена, себестоимость), KPI (позиции/склад по себест./мало на складе), подсветка низкого остатка, фильтр по категории. Склад: операции Поставка(+)/Списание(−)/Инвентаризация(=) — модалка меняет остаток товара и пишет в журнал операций; фильтр. Store: src/lib/goods-store.ts (data/goods.json {products, ops}), API `/api/goods` (POST action product или kind:stock). Файл: sections/goods.tsx (Products+Stock переписаны, Payroll там же не тронут). Проверено (поставка +10 меняет остаток).

**Группа «Товары» готова (разделы 19–20).**

**Раздел 21 «Зарплата» (готово 2026-08-03):** ставка % с услуг по каждому мастеру + премия/штраф (редактируемо, сохраняется), начисление считается из реальных продаж (`/api/sales`, матч по короткому имени мастера `Фамилия Имя`), к выплате = выручка×% + премия − штраф; KPI (итого к выплате, мастеров, средняя ставка). Store: src/lib/payroll-store.ts (data/payroll.json {schemes, adjust} по masterId), API `/api/payroll`. Файл: Payroll в sections/goods.tsx. Проверено. NB: продажи из модалки «Оформить продажу» пишут master = короткое имя, совпадает с матчем.

**Раздел 22 «Бонусы» (готово 2026-08-03):** бонусная программа через useSettings('bonuses') — активна, % кэшбэка, бонусы на ДР, макс. оплата бонусами; Сохранить пишет на сервер. Файл: Bonuses в sections/loyalty.tsx. Проверено.

**Разделы 23–26 (готово 2026-08-03, сделаны разом по просьбе Павла):** Сертификаты (CRUD: выпуск/удаление, KPI сумма/не погашено), Абонементы (CRUD карточки), Акции (CRUD таблица), Чаевые (реальный QR через qrcode на /tips + скачивание). Универсальный store именованных списков: src/lib/collections-store.ts (data/collections.json {certificates, seasonTickets, shares}), API `/api/collections?name=` (GET + POST add/delete), общий хук useCollection + FormModal в sections/loyalty.tsx. Проверено (add cert, delete share).

**Группа «Лояльность» готова (разделы 22–26).**

**Группа «Настройки» готова (разделы 27–29, 2026-08-03):** Профиль (поля студии + регион через useSettings('profile'), сохранение), Доступы (CRUD сотрудников с ролями Владелец/Администратор/Мастер, тумблеры финансы/чужие записи; store src/lib/permissions-store.ts + API `/api/staff`), Телефония (тумблеры через useSettings('telephony'), дефолты добавлены). Файл: sections/settings.tsx (переписан). Проверено.

## ✅ ВСЕ 29 РАЗДЕЛОВ ГОТОВЫ (2026-08-03)
Полная проверка: все 31 роут (форма + admin + 29 разделов) отвечают 200/307, сборка чистая. Каждый раздел — реальная логика + сохранение на сервер (файловые сторы в /root/booking-app/data/, заменить на Prisma+Postgres при масштабировании). Живые связки: запись→журнал, продажи→аналитика→зарплата, склад→товары, клиенты→возвращаемость/рассылки. PWA (устанавливается на телефон), клиентская форма записи с сохранением.

**2026-08-03: приложение поднято на HTTPS — https://zapis.gudhab.com 🚀** Павел добавил DNS A-запись (zapis→46.8.79.53). Настроил nginx (sites-available/zapis.gudhab.com, прокси на :3002), выпустил Let's Encrypt сертификат через `PATH=$PATH:/usr/sbin certbot --nginx -d zapis.gudhab.com --redirect` (плагин nginx не находил бинарь без PATH). Cert до 2026-11-01, авто-renew. HTTP→HTTPS редирект, PWA-манифест/sw/api работают по HTTPS, PWA теперь устанавливается по-настоящему. Данные почищены (bookings/schedule пустые). Проверен сквозной цикл записи по HTTPS. Порт 3002 в ufw пока открыт (можно закрыть — доступ идёт через nginx :443; оставил на всякий).

**2026-08-04 UI-фиксы по фидбеку Павла (на телефоне):** (1) safe-area отступ сверху — панель/бургер не под чёлкой (Sidebar mobile bar + BookingFlow header, env(safe-area-inset-top)); (2) кэш: HTML отдавался с s-maxage=31536000 (год!) → в nginx zapis.gudhab.com добавил Cache-Control no-cache для HTML/sw/manifest, immutable для /_next/static/; SW переписан на network-first v3 + авто-reload (PWA.tsx); (3) журнал «плыл» на телефоне (2-осевая прокрутка + sticky jank) → сделал адаптивным: на мобиле (matchMedia max-width:1023px) лента выбора мастера + один мастер во всю ширину, только вертикальный скролл; десктоп — полная сетка. Всё подтверждено Павлом.

**2026-08-04 Раздел «Авторизация» (п.1 из плана, готово):** вход по паролю в /admin. middleware.ts защищает /admin/* и /api/* КРОМЕ /api/auth и POST /api/bookings (клиентская форма публична). Страница /login, /api/auth (POST verify пароль→HttpOnly cookie gk_auth=AUTH_TOKEN на 30 дней; DELETE=выход), кнопка «Выйти» в сайдбаре. Секреты в systemd unit booking-app.service: **ADMIN_PASSWORD=galereya2026** (временный, Павел сменит), AUTH_TOKEN=<random>. Сменить пароль: отредактировать Environment=ADMIN_PASSWORD в /etc/systemd/system/booking-app.service + `systemctl daemon-reload && systemctl restart booking-app`. Проверено: /admin без куки→307 /login, /api/* →401, публичная запись POST /api/bookings→200, верный пароль→доступ.

**2026-08-04 Пароль админки сменён** на постоянный (Павел прислал, я поставил в systemd Environment="ADMIN_PASSWORD=..."; старый galereya2026 отозван). Оба сообщения с паролями Павел удалил из чата.

**2026-08-04 п.2 WhatsApp-уведомления (GREEN-API) — механика готова, ждёт ключей:** src/lib/whatsapp.ts (ключи в data/whatsapp.json, НЕ отдаются клиенту; sendMessage через api.green-api.com/waInstance{id}/sendMessage/{token}; шаблон подтверждения записи; notifyBookingCreated читает настройки notifications.createBooking). API `/api/whatsapp` (GET статус без токена, POST сохранить ключи / action:test). Хук в POST /api/bookings — при онлайн-записи шлёт подтверждение (тихий no-op без ключей, запись не ломается). UI: карточка в разделе «Уведомления» (WhatsappCard в notify.tsx) — поля idInstance+apiToken, «Сохранить ключи», «Тест» на номер. Проверено: без ключей всё работает вхолостую, сохранение ключей→configured. **Активация: Павел регистрируется на green-api.com, получает idInstance+apiToken, вводит в разделе Уведомления (или шлёт мне), жмёт Сохранить+Тест → подтверждения пойдут клиентам.** Дал Павлу ссылку green-api.com и инструкцию.

**2026-08-04: WhatsApp переведён на официальный Meta Cloud API** (Павел спросил про блокировки — GREEN-API московский, а РКН блокирует WhatsApp в РФ; студия/клиенты в КЗ, проблема только в отправителе; выбрали официальный Cloud API). whatsapp.ts переписан под Graph API v21.0: config {phoneNumberId, accessToken, templateName, langCode} в data/whatsapp.json; sendText (24ч-окно), sendTemplate (проактив), sendTest (hello_world), notifyBookingCreated шлёт шаблон [имя, услуга, дата, время] при онлайн-записи если templateName задан. API `/api/whatsapp` (GET статус без токена; POST сохранить/test; пустой accessToken при сохранении = оставить прежний). UI-карточка в Уведомлениях: Phone Number ID, Access Token, Шаблон, Язык, Сохранить, Тест (hello_world). Проверено: без ключей no-op, запись не ломается. Дал Павлу шаги настройки Meta (developers.facebook.com → app Business → продукт WhatsApp → Phone Number ID + временный токен 24ч + добавить свой номер в тестовые получатели → ввести у нас → Тест). Для боевого: свой номер, бизнес-верификация, шаблон booking_confirmation с 4 переменными на одобрение, постоянный токен (System User).

**Осталось (план Павла):** активировать WhatsApp по ключам Meta от Павла + создать/одобрить шаблон; п.3 боевая БД Postgres+Prisma; п.4 кабинет мастера (отдельный вход); п.5 доводка UI; напоминания о визите (нужен планировщик/cron). При переезде на боевой домен — nginx+certbot. Напомнить Павлу сменить пароль DIKIDI ([[dikidi-access]]). Затем: Товары (Товары, Склад), Зарплата, Лояльность (Бонусы, Сертификаты, Абонементы, Чаевые, Акции), Настройки (Профиль, Доступы, Телефония). NB: Павел иногда пишет в ЛИЧКУ (1097461410) вместо топика 114 — отвечаю в топик 114 (лог проекта), слежу за обоими. НЕ сделано глобально: авторизация/роли, кабинет мастера, WhatsApp (GREEN-API), HTTPS-домен (ждём от Павла) для настоящей установки PWA, замена файлового JSON на Prisma+Postgres. Жду домен и продолжаю разделы.

Аудит: research/dikidi-audit.md, ТЗ: research/booking-app-tz.md (+ exports/booking-app-tz.pdf), доступ DIKIDI: [[dikidi-access]]. Связано: [[hab-project]], [[fp-babichnail]].

---
## СВЕЖИЙ СТАТУС 2026-08-04 (перекрывает противоречия выше)
Проект живёт на **https://zapis.gudhab.com** (HTTPS, PWA). Готово: все 29 разделов админки с реальной логикой и файловыми сторами (/root/booking-app/data/); авторизация админа (ADMIN_PASSWORD в systemd, пароль Zap2026GK@# — постоянный); **кабинет мастера /master** (MASTER_PASSWORD=master2026 временный, роль master через middleware, мастер видит своё расписание + статусы, изолирован от /admin); WhatsApp-механика под Meta Cloud API готова, ждёт ключи (Павел застрял на регистрации Meta — устройство флажат, пробует с телефона/ждёт).
**Следующее:** активировать WhatsApp (ключи Meta + шаблон booking_confirmation); напоминания о визите (cron); п.3 боевая БД Postgres+Prisma вместо файлов; UI-доводка; server-side назначение мастера "any"-записям; per-master коды. Порт 3002 в ufw открыт (доступ через nginx :443). Напомнить Павлу сменить пароль DIKIDI ([[dikidi-access]]).

## 2026-08-04 (2) Фидбек Павла отработан
- **Журнал: вернул полную сетку** всех мастеров на одном экране (убрал мобильный режим «один мастер»). Колонки уже на телефоне (w-32/sm:w-40/lg:w-44). Он это просил ради обзора занятости для ручного распределения и комбо-процедур (два мастера на одного клиента — видно в соседних колонках). «Резину» добил: overscrollBehavior:none + touchAction:pan-x pan-y + GPU-хинты (willChange) на sticky-шапках; body overscroll:none.
- **Фото сотрудников**: загрузка в редакторе Сотрудники (MastersAdmin), поле Master.photo; аватары показываются в журнале (photoMap через /api/masters), на карточках мастеров. **Логотип студии**: загрузка в Профиле (settings profile.logo), показывается в сайдбаре.
- **Загрузки**: POST /api/upload (base64→файл, до 3МБ, png/jpg/webp) пишет в **/var/www/booking-uploads** (НЕ в /root — nginx www-data не читает /root), nginx отдаёт по `/uploads/` (location alias добавлен в zapis.gudhab.com). NB: файлы вне гит-бэкапа воркспейса.
- Осталось из фидбека на будущее: настоящая связанная «комбо»-запись (сейчас — две отдельные записи в двух колонках); фото на публичной форме записи (нужен public-эндпоинт настроек, /api/settings под auth). Комбо и логотип на клиентской форме — если Павел попросит.
